# 🎧 Voice Overlay — Minimal In‑Game Audio Player

یک ابزار سبک و پرتابل برای پخش سریع ویس‌ها روی بازی‌هایی مثل CS2
با پشتیبانی از Hotkey سراسری، شفافیت بالا (Overlay)، و پشتیبانی از فرمت‌های MP3، OGG و Opus.

## 🚀 ویژگی‌ها

- 🪶 رابط کاربری مینیمال (HUD‑Style)
- 🎮 Hotkey‌های سراسری بدون نیاز به فوکوس:
  - `Ctrl + Alt + ↑ / ↓` پیمایش بین صداها
  - `Ctrl + Alt + P` پخش/توقف صدا
  - `Ctrl + Alt + R` تازه‌سازی لیست
- 🔊 پخش مستقل با استفاده از `ffplay` (بدون نیاز به Player خارجی)
- 🧱 پشتیبانی داخلی از فونت فارسی **Vazirmatn**
- 💎 Overlay شفاف با گوشه‌های گرد، راست‌چین، و click‑through
- 🗂️ پشتیبانی کامل از فرمت‌های `MP3`, `OGG`, `WAV`, `Opus`

## 📦 ساخت Structure پروژه

```

VoiceOverlay/
│
├─ voice_overlay.py
├─ Vazirmatn-Regular.ttf
├─ ffmpeg/
│ ├─ ffplay.exe
│ ├─ ffmpeg.exe
│ └─ ffprobe.exe
│
└─ voices/
├─ test1.ogg
├─ test2.mp3

```

> 🔸 صداهای جدید را در هر زمان می‌توانید به پوشه‌ی `voices/` اضافه کنید.
> برنامه هنگام اجرا (یا با میانبر `Ctrl+Alt+R`) خودش لیست را به‌روزرسانی می‌کند.

## ⚙️ نصب برای توسعه‌دهنده‌ها

### ۱. پیش‌نیازها

- **Python 3.9 تا 3.12** (نسخه 3.13 هنوز با برخی پکیج‌های صوتی ناسازگار است)
- نصب ماژول‌ها:
  ```bash
  pip install -r requirements.txt
  ```

````

### ۲. اجرای نسخه توسعه

```bash
python voice_overlay.py
```

> مطمئن شوید که `ffplay.exe` داخل مسیر `./ffmpeg/` وجود دارد.



## 📦 بیلد نهایی (ساخت EXE پرتابل)

برای ساخت فایل اجرایی مستقل ویندوز:

```bash
pyinstaller --onefile --noconsole --add-data "Vazirmatn-Regular.ttf;." --add-data "ffmpeg;ffmpeg" --add-data "voices;voices" voice_overlay.py
```

**توضیحات فلگ‌ها:**

| فلگ           | توضیح                                       |
| - | - |
| `--onefile`   | تولید یک فایل exe تک‌فایلی                  |
| `--noconsole` | اجرای بدون پنجره کنسول                      |
| `--add-data`  | افزودن فایل‌های اضافی (فونت، صداها، ffmpeg) |
| `;`           | جداکننده مسیر در ویندوز (در لینوکس `:` است) |

📁 فایل خروجی در مسیر:

```
dist/voice_overlay.exe
```



## 💽 اجرای نسخه پرتابل

ساختار نهایی برای انتشار:

```
voice_overlay.exe
voices/
```

کاربر فقط کافی‌ست این دو را در یک فولدر داشته باشد و اجرا کند ✅
نیازی به نصب Python یا FFMPEG سیستم نیست؛ همه چیز داخلی پکیج شده است.



## ⛓️ بیلد اتوماتیک (اختیاری)

برای راحتی بیلد مجدد، می‌توانید فایل زیر را در روت پروژه بسازید:

**auto-build.bat**

```bat
@echo off
echo 🔧 Building Voice Overlay...
pyinstaller --clean --onefile --noconsole ^
 --add-data "Vazirmatn-Regular.ttf;." ^
 --add-data "ffmpeg;ffmpeg" ^
 --add-data "voices;voices" ^
 voice_overlay.py
echo ✅ Build complete! Output: dist\voice_overlay.exe
pause
```



## 🧩 رفتار برنامه

- در هر بار اجرا فایل‌های داخل `voices/` را می‌خواند.
- در صورت اجرای Hotkeyهای تعیین‌شده در هر محیط (حتی داخل بازی)، صدا را با `ffplay` پخش می‌کند.
- پخش صدای جدید به‌صورت اتوماتیک صدای قبلی را قطع می‌کند.



## 🎨 رابط کاربری (UI)

- **Foreground رنگ:** سفید
- **Background:** `#111111` (تیره، شفافیت 0.7)
- **فونت:** `Vazirmatn-Regular.ttf` (داخلی)
- **کادر انتخاب (Selection):** مستطیل روشن با glow animation
- **Text Wrap:** 320px راست‌چین شده برای نمایش متن‌های بلند



## 🧰 عیب‌یابی رایج

| خطا                                             | علت                          | راه‌حل                                                          |
| -- | - |  |
| `[WinError 2] ffmpeg not found`                 | مسیر `ffmpeg` یافت نشده      | اطمینان از وجود فولدر `ffmpeg/` و فایل `ffplay.exe` در کنار EXE |
| `ModuleNotFoundError: audioop` (در Python 3.13) | حذف شدن ماژول از Python 3.13 | از Python ≤3.12 استفاده کنید                                    |
| `IndentationError` در هنگام ساخت audioop.py     | تورفتگی اشتباه در فایل فیک   | مطمئن شوید اولین خط از چپ شروع شود                              |



## 🧠 یادداشت توسعه‌دهنده

- هدف پروژه: **پخش سریع و ساده ویس درون بازی بدون مزاحمت رابط یا نیاز به فوکوس.**
- طراحی HUD سبک CS2 با تمرکز بر **جلوه‌ی تمیز، عملکرد فوری و کم‌ترین دیلی در پخش.**
- برای بهینه‌سازی عملکرد توصیه می‌شود از فرمت‌های `OGG/Vorbis` یا `Opus` استفاده شود.



## 📜 لایسنس

MIT License – استفاده، ویرایش و انتشار آزاد است.
ذکر نام پروژه در صورت استفاده‌ی تجاری پیشنهاد می‌شود.



## ✨ سازندگان

- **Dev:** شما (`@<GitHub-Username>`)
- **UI/UX:** طراحی مینیمال با الهام از HUD بازی‌های FPS
- **Build:** PyInstaller + FFmpeg embedded + Vazirmatn font



```

```
````
